# KDB+ Ticker System - Symbol Configuration
# Format: SYMBOL,PRIORITY,MAX_MEMORY_MB,RESTART_POLICY

# Major cryptocurrencies
ethusdt,high,512,always
btcusdt,high,512,always
bnbusdt,medium,256,always

# Additional symbols (uncomment to enable)
# solusdt,medium,256,always
# adausdt,medium,256,always
# xrpusdt,medium,256,always
# dotusdt,low,128,on-failure
# maticusdt,low,128,on-failure
# linkusdt,low,128,on-failure
# uniusdt,low,128,on-failure

# Stablecoins (lower priority, less memory)
# usdcusdt,low,64,on-failure
# busdusdt,low,64,on-failure
# daiusdt,low,64,on-failure

# Configuration notes:
# - Priority: high|medium|low (affects CPU scheduling)
# - Max Memory: Memory limit in MB per symbol
# - Restart Policy: always|on-failure|unless-stopped
# - Lines starting with # are ignored 